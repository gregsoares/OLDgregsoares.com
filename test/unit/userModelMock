import { User } from '../../models/userModel';

const mockUser = jest.fn( () => {
  new User({
    'name': 'Greg',
    'age': 30,
    'subscribed': true,
  });
});
test('Mocking user models to check against DB', () => {

});
